import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI Decoder",
};

export default function AIDecoderLayout({ children }: { children: React.ReactNode }) {
  return children;
}
